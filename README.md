# API REST LABORATORY FOR LEMONCODE MASTER FRONT END

The laboratory is a simple React application that consumes the [Rick and Morty API](https://rickandmortyapi.com/) to display a list of characters and their details.

**To install the dependencies, run:**

```bash
npm install
```

**To start the application, run:**

```bash
npm start
```
